import { StrikeoverPipe } from './strikeover.pipe';

describe('StrikeoverPipe', () => {
  it('create an instance', () => {
    const pipe = new StrikeoverPipe();
    expect(pipe).toBeTruthy();
  });
});
